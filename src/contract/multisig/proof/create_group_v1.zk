k = 11;
field = "pallas";

constant "CreateGroupV1" {
    EcFixedPointBase NULLIFIER_K,
}

witness "CreateGroupV1" {
    // Group parameters
    Base group_id,
    Base threshold,
    Base total_keys,

    // Transaction binding
    Base tx_commitment,
    Base tx_nonce,
}

circuit "CreateGroupV1" {
    // Threshold must be >= 1
    range_check(8, threshold);

    // Threshold must be <= total_keys (enforced by caller)

    // Transaction binding
    tx_binding = poseidon_hash(tx_commitment, tx_nonce);
    constrain_instance(tx_binding);
    constrain_instance(tx_nonce);

    // Public outputs
    constrain_instance(group_id);
    constrain_instance(threshold);
    constrain_instance(total_keys);
}
