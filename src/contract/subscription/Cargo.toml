[package]
name = "subscription_contract"
version = "0.1.0"
edition = "2021"
authors = ["DarkFi contributors"]
description = "DarkFi Subscription Contract - Member subscription service with DAO treasury and endowment fund"
license = "AGPL-3.0-only"

[dependencies]
darkfi-sdk = { path = "../../../sdk", features = ["no-entrypoint"] }
darkfi_serial = { path = "../../../serial" }
pasta = { path = "../../../pasta" }

[features]
default = ["no-entrypoint"]
no-entrypoint = ["darkfi-sdk/no-entrypoint"]
client = ["darkfi-sdk/client"]