# Status report — 2026-09-26

Generated by `scripts/report-status.sh`. **Every figure below comes from a run this script made in this run**, and every verdict names the instrument that produced it. Nothing here is quoted from memory, from another document, or from a previous report.

## Subject

| | |
|---|---|
| HEAD | `ad06574523102182eabc3116aae4c7ff8916c200` |
| Branch | `linear-master` |
| Working tree | 1 modified path(s) — the report describes the tree, not a commit |
| Toolchain | `channel = "1.95.0"` · `rustc 1.95.0 (59807616e 2026-04-14)` |
| Budget | static instruments only: no cargo, make, lake, docker or rustc. **No artifact was rebuilt, so the genesis pin cannot have moved as a result of this run.** |

**Which tree this describes, stated because a committed report cannot name its own commit.** The subject above is HEAD at generation time, and every instrument ran against that tree *plus the modifications listed*. This file does not exist in that HEAD — it is committed as the change on top of it — so a reader reconstructing the subject should take that commit and apply the report's own commit, or simply note that the modified-path count is the rest of the delta. The alternative, naming the commit that carries the file, is not available from inside the run that writes it.

**This report does not establish everything it reports on.** 8 criteria read NOT ESTABLISHED because this budget cannot decide them; each names the exact command that would. That is a result, not a gap (R7).

## Verdicts

`source` is load-bearing: **derived** means the verdict was computed from an instrument's exit in this run; **declared** means it rests on a reading and says so. Hiding which is which is how a report overclaims.

### Genesis

| ID | Criterion | Verdict | source | Instrument |
|---|---|---|---|---|
| G1 | The ceremony reads no ambient authority — no clock, RNG, network, or undeclared file | **NOT ESTABLISHED** | declared | `init_genesis` → `build_genesis_block` → `build_genesis_deployment_txs` |
| G2 | Embedded contract code is a quoted argument, not an effect | **MET** | derived | the `include_bytes!` probe: the deployment table is the only route the wasm enters by |
| G3 | The ceremony's stages are pure transitions whose composition is a pure transition | **NOT ESTABLISHED** | declared | — |
| G4 | Totality — no panic path reaches the genesis artifacts | **NOT MET** | derived | `contrib/wasm_artifact_check.sh --genesis` |
| G5 | Determinism — the same inputs yield the same hash | **NOT ESTABLISHED** | declared | — |
| G6 | Reproducibility — the artifact is a function of the source, not of the host | **NOT MET** | derived | `wasm_artifact_check.sh --genesis` + the absolute-path probe |
| G7 | The pin is populated, committed, and clean against HEAD | **MET** | derived | `git status --porcelain` and a placeholder count on the pin file |
| G8 | The pin is CURRENT — this build computes the hash the pin records | **NOT ESTABLISHED** | declared | — |
| G9 | The nine genesis ids agree across their three enumerations, in consensus order | **MET** | derived | parse `execution.rs`, `lib.rs` and `contract_id.rs` and compare |
| G10 | The genesis model conforms to the Lean model | **MET** | derived | `contrib/genesis_model_conformance.sh` |
| G11 | The nine genesis contracts pass their heavyweight tests | **NOT ESTABLISHED** | declared | — |

**G1 — The ceremony reads no ambient authority — no clock, RNG, network, or undeclared file**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: the genesis path is a reading task; `genesis.md:153-165` states the clause. declared, not derived: no instrument decides it
- to settle it: `read the three clauses at doc/src/arch/genesis.md:153-165 against the path`

**G2 — Embedded contract code is a quoted argument, not an effect**

- verdict: **MET** (derived)
- evidence: the `bin/dwowd/src/lib.rs` deployment table enumerates 9 contract wasm paths; `include_bytes!` of a `.wasm` elsewhere in non-test src/ or bin/: none. The one hit that exists is a test (`src/contract/deployooor/tests/artifact_validates.rs`), which embeds the artifact to validate it and is not on the genesis path; test files are excluded by path and the exclusion is stated here rather than left silent

**G3 — The ceremony's stages are pure transitions whose composition is a pure transition**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: no instrument; this is the third normative clause and it is a reading
- to settle it: `read genesis.md:153-165 clause 2 against the stage boundaries`

**G4 — Totality — no panic path reaches the genesis artifacts**

- verdict: **NOT MET** (derived)
- evidence: DIRTY  src/contract/deployooor/dwow_deployooor_contract.wasm  (1494299 bytes) / panic machinery: panic_bounds_check(1) rust_begin_unwind(1) / embedded paths:
- to settle it: `cargo test --release -p dwowd -- test_heavyweight_<c>  (x9)`

**G5 — Determinism — the same inputs yield the same hash**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: the test that decides it is behind a build, which constraint 1 excludes
- to settle it: `cargo test -p dwowd --all-features test_genesis_determinism`

**G6 — Reproducibility — the artifact is a function of the source, not of the host**

- verdict: **NOT MET** (derived)
- evidence: gate: DIRTY  src/contract/deployooor/dwow_deployooor_contract.wasm  (1494299 bytes) / panic machinery: panic_bounds_check(1) rust_begin_unwind(1) / embedded paths:  ·  purity probe (absolute host paths): deployooor=0 native_token=0 promissory_note=0 identity=0 oracle=0 attestation=0 purse=0 box=0 multisig=0
- to settle it: `see OBL-C140 and the row minted by this pass — the two readings differ and both matter`

**G7 — The pin is populated, committed, and clean against HEAD**

- verdict: **MET** (derived)
- evidence: clean=yes; placeholder=no; last moved: 688565df10 purse manifest: the public_inputs lists are load-bearing, not leftovers — reverted, and why

**G8 — The pin is CURRENT — this build computes the hash the pin records**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: only the test asserts it; a static reading cannot decide currency
- to settle it: `cargo test -p dwowd --all-features genesis_pin_is_current`

**G9 — The nine genesis ids agree across their three enumerations, in consensus order**

- verdict: **MET** (derived)
- evidence: execution.rs (9) id constants: ['deployooor', 'native_token', 'promissory_note', 'identity', 'oracle', 'attestation', 'purse', 'box', 'multisig']
  lib.rs        (9): ['deployooor', 'native_token', 'promissory_note', 'identity', 'oracle', 'attestation', 'purse', 'box', 'multisig']
  contract_id.rs(9): ['deployooor', 'native_token', 'promissory_note', 'identity', 'oracle', 'attestation', 'purse', 'box', 'multisig']
  display labels in execution.rs (compared as labels, not ids): ['Deployooor', 'NativeToken', 'PromissoryNote', 'Identity', 'Oracle', 'Attestation', 'Purse', 'Box', 'MultiSig']
  agree=True

**G10 — The genesis model conforms to the Lean model**

- verdict: **MET** (derived)
- evidence: genesis_model_conformance: model and Rust agree

**G11 — The nine genesis contracts pass their heavyweight tests**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: needs a release build of dwowd and nine proving runs — outside this budget
- to settle it: `cargo test --release -p dwowd -- test_heavyweight_deployooor … (x9)`

### Consensus

| ID | Criterion | Verdict | source | Instrument |
|---|---|---|---|---|
| C1 | Zero OPEN CRITICAL register rows, under the declared counting | **MET** | derived | Status-column census of the register |
| C2 | The open HIGH rows are enumerated, each with its subject | **6 rows** | derived | Status-column census of the register |
| C3 | The umbrella's Tier-1 gates are all green | **NOT ESTABLISHED** | declared | `scripts/run-all-tests.sh` |
| C4 | Every gate that runs can fail — each ships a control (R8) | **NOT MET** | derived | the `--self-test` census over every gate script |
| C5 | No gate exists that is red and wired by nothing | **NOT MET** | derived | diff the gate scripts on disk against `run_gate` in the umbrella |
| C6 | Committed artifacts match the sources they carry | **MET** | derived | `scripts/check-artifact-freshness.sh` |
| C7 | The circuit invariants hold (metadata, domain separation, derivation, transcription, index, fidelity) | **MET** | derived | the six circuit gates |
| C8 | The L1 wire carries no value that reaches call params with no observer | **MET** | derived | `scripts/check-l1-wire-conformance.sh` + OBL-C145 |
| C9 | exec writes no state; apply reads none | **MET** | derived | `scripts/check-phase-host-functions.sh` |
| C10 | Consensus value cannot be minted through a contract | **MET** | derived | the register's C-severity rows |
| C11 | The heavyweight contract suite is green | **NOT ESTABLISHED** | declared | — |
| C12 | Finality is verified, not merely enforced | **NOT ESTABLISHED** | declared | — |

**C1 — Zero OPEN CRITICAL register rows, under the declared counting**

- verdict: **MET** (derived)
- evidence: open C-severity rows: 0. CAVEAT, in the same cell on purpose: one C-severity row is dispositioned ACCEPTED-WITH-REASON rather than closed and the register's prose still calls it open — OBL-C80 — so '0 open critical' is true of the row statuses and misleading without that sentence

**C2 — The open HIGH rows are enumerated, each with its subject**

- verdict: **6 rows** (derived)
- evidence: OBL-C92* (OPEN); OBL-C97* (PARTLY); OBL-C133* (OPEN); OBL-C143* (OPEN); OBL-C145* (PARTLY); OBL-C146* (OPEN)

**C3 — The umbrella's Tier-1 gates are all green**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: it contains `make test` and the Lean build. tier1 gates wired=50 controls=7; this report ran the static subset and names the remainder
- to settle it: `bash scripts/run-all-tests.sh`

**C4 — Every gate that runs can fail — each ships a control (R8)**

- verdict: **NOT MET** (derived)
- evidence: 5 of 29 gate scripts mention a self-test; notably `scripts/check-doc-index.sh` does not, while `run-all-tests.sh:71` states "a gate whose control cannot make it fail is not a gate"
- to settle it: `add a --self-test to check-doc-index.sh and wire it as an umbrella control`

**C5 — No gate exists that is red and wired by nothing**

- verdict: **NOT MET** (derived)
- evidence: unwired gate scripts:
    - contrib/ci/check_fee_guardrails.sh
    - contrib/ci/check_heavyweight_coverage.sh
    - contrib/ci/check_sync_conformance.sh
    - contrib/genesis_model_conformance.sh
    - contrib/wasm_artifact_check.sh
- to settle it: `see OBL-C137 — the class is recorded; these are its current instances`

**C6 — Committed artifacts match the sources they carry**

- verdict: **MET** (derived)
- evidence: PASS: 32 artifacts fresh, all live namespace constants resolve

**C7 — The circuit invariants hold (metadata, domain separation, derivation, transcription, index, fidelity)**

- verdict: **MET** (derived)
- evidence: circuit_metadata_alignment=MET; circuit_domain_separation=MET; circuit_instance_derivation=MET; circuit_transcription=MET; circuit_index=MET; circuit_fidelity=MET

**C8 — The L1 wire carries no value that reaches call params with no observer**

- verdict: **MET** (derived)
- evidence: PASS: 10 declared wire leak(s) in promissory_note, box, purse; none new, none stale. Removing a field from the wire is what retires each entry.  ·  the gate is green; OBL-C145 is PARTLY — both are true and neither replaces the other

**C9 — exec writes no state; apply reads none**

- verdict: **MET** (derived)
- evidence: PASS: no unexcepted phase violations (28 host functions' ACLs parsed from the runtime)

**C10 — Consensus value cannot be minted through a contract**

- verdict: **MET** (derived)
- evidence: 126 of 194 rows closed/satisfied/fixed/mechanized; the C-severity open set is empty (C1's caveat applies)

**C11 — The heavyweight contract suite is green**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: the register's 2026-09-25 sweep records ten red; the report cites the register rather than restating it as this run's measurement
- to settle it: `make test  (or the targeted cargo test --release -p dwowd -- test_heavyweight_<c>)`

**C12 — Finality is verified, not merely enforced**

- verdict: **NOT ESTABLISHED** (declared)
- evidence: a reading task over caribina::verify_anchor's callers; the register's OBL-C63–C71 record the closures and OBL-C68 has no characterization test and says so
- to settle it: `read doc/src/arch/caribina.md's integration table against the callers`

### Documentation

| ID | Criterion | Verdict | source | Instrument |
|---|---|---|---|---|
| D1 | The index is coherent in both directions | **MET** | derived | `scripts/check-doc-index.sh` |
| D2 | Every cited authority resolves | **MET** | derived | `scripts/check-authority-resolves.sh` |
| D3 | Every file the register cites exists at HEAD | **MET** | derived | `scripts/check-register-artifacts.sh` |
| D4 | Every register row carries a status from the vocabulary | **MET** | derived | `scripts/register-status.sh --check` |
| D5 | No live claim in the tree contradicts the tree | **NOT MET** | derived | the probes below; this class has NO gate |
| D6 | A register row's cited line numbers and symbols still resolve | **NOT MET** | derived | the probes below; no instrument exists for this class |
| D7 | The root-cause taxonomy is coherent and its legacy ids are aliased | **MET** | declared | `check-doc-index.sh` check 6 + reading |

**D1 — The index is coherent in both directions**

- verdict: **MET** (derived)
- evidence: PASS: doc index coherent — 250 documents, 198 obligations, all cross-references resolve

**D2 — Every cited authority resolves**

- verdict: **MET** (derived)
- evidence: PASS: every cited section (28) and clause id (26) resolves

**D3 — Every file the register cites exists at HEAD**

- verdict: **MET** (derived)
- evidence: PASS: every in-scope register citation resolves against HEAD

**D4 — Every register row carries a status from the vocabulary**

- verdict: **MET** (derived)
- evidence: OK: every row carries one status from the vocabulary in its Status column (198 rows walked).

**D5 — No live claim in the tree contradicts the tree**

- verdict: **NOT MET** (derived)
- evidence: a live claim that the tree contradicts, found by this run: the two build levers genesis.md and the artifact gate's header both say are in force. `strip` in any Cargo.toml: NONE; `location-detail` in any build file: only in prose
- to settle it: `see the row minted by this pass; the repair is a decision, not an edit`

**D6 — A register row's cited line numbers and symbols still resolve**

- verdict: **NOT MET** (derived)
- evidence: `const MAX_BLOCK_SIZE` in src/ or bin/: no hit — while it is still cited by live rows. check-register-artifacts.sh states in its own header that it checks no symbol and strips line suffixes
- to settle it: `see the row minted by this pass`

**D7 — The root-cause taxonomy is coherent and its legacy ids are aliased**

- verdict: **MET** (declared)
- evidence: RC1–RC12 canonical at safety.md:111-129; every legacy scheme aliased at :1056-1101; check 6 resolves every `safety.md RC<n>` citation (it passes)

## The instrument census

25 green, 4 red, 0 missing — over 30 instruments, all static. Deliberately among them: gates the umbrella wires only as their own self-test, and gates wired nowhere at all. This report runs them because otherwise their verdicts enter no record — see C5.

Not run, and not counted as gates: `add_missing_license_headers.sh` (writes every .rs file — a tool, not a check); `app_android-battery-sampler.sh` (an unrelated app aid); `clippy_critical_counts.sh` (a ratchet counter, not a defect gate); `clippy_totality_counts.sh` (a ratchet counter, not a defect gate); `ctags.sh` (builds a tag index); `dependency_setup.sh` (installs dependencies); `length_cast_counts.sh` (a ratchet counter, not a defect gate (in INSTRUMENTS as such)); `measure_rss.sh` (a memory-measurement harness (a counter of a running process)); `test_inventory.sh` (enumerates tests; it asserts nothing).

| Instrument | Verdict | Its own last line, quoted |
|---|---|---|
| `doc_index` | **MET** | PASS: doc index coherent — 250 documents, 198 obligations, all cross-references resolve |
| `register_artifacts` | **MET** | PASS: every in-scope register citation resolves against HEAD |
| `authority_resolves` | **MET** | PASS: every cited section (28) and clause id (26) resolves |
| `artifact_freshness` | **MET** | PASS: 32 artifacts fresh, all live namespace constants resolve |
| `register_status_check` | **MET** | OK: every row carries one status from the vocabulary in its Status column (198 rows walked). |
| `circuit_metadata_alignment` | **MET** | OK:   attestation/attest_slash — 2 constrain_instance, 2 metadata pushes (ATTESTATION_CONTRACT_ZKAS_ATTEST_SLASH_NS_V2; no client to_vec found) |
| `circuit_domain_separation` | **MET** | PASS: no unexcepted domain-separation findings (4 adjudicated in script/circuit_domain_exceptions.txt) |
| `circuit_instance_derivation` | **MET** | [32mOK:[0m  every constrain_instance in 178 circuits is derived, bound, redundant, or declared free, and every comparison operand is range-checked |
| `circuit_transcription` | **MET** | OK: proofs/lean/src/Transcribed.lean matches the sources — 178 circuits, 2686 statements, 21 ignored opcode calls, 11 hold, 167 fail |
| `circuit_index` | **MET** | OK: proofs/lean/src/CircuitIndex.lean matches the sources — 12 pair(s) resolved, 2 to no circuit |
| `circuit_fidelity` | **MET** | [32mOK:[0m  every circuit's compiled opcode count and identity match its source (178 checked) |
| `phase_host_functions` | **MET** | PASS: no unexcepted phase violations (28 host functions' ACLs parsed from the runtime) |
| `pubkey_binding` | **MET** | PASS: no unexcepted vacuous bindings (178 circuit(s) scanned; 52 adjudicated in script/circuit_pubkey_binding_exceptions.txt) |
| `client_params_alignment` | **MET** | OK: no client-declared params codec; 2 rule-(A) shadow(s) excepted (2 declared). |
| `hidden_tests` | **MET** | PASS: every test that does not run by default is declared. |
| `metadata_arms` | **MET** | PASS: every empty get_metadata arm is declared. |
| `coinbase_classifier` | **MET** | OK: 3 use(s) of the coinbase probe are position-gated or prose; every accept-path |
| `wallet_kernel` | **MET** | no violations |
| `l1_wire_conformance` | **MET** | PASS: 10 declared wire leak(s) in promissory_note, box, purse; none new, none stale. Removing a field from the wire is what retires each entry. |
| `zk_bins` | **MET** | OK: src/contract/attestation/proof/attest_slash.zk.bin |
| `genesis_model_conformance` | **MET** | genesis_model_conformance: model and Rust agree |
| `wasm_artifact_genesis` | **NOT MET** | DIRTY  src/contract/deployooor/dwow_deployooor_contract.wasm  (1494299 bytes) / panic machinery: panic_bounds_check(1) rust_begin_unwind(1) / embedded paths: |
| `barb_alphabet` | **MET** | python Barb == sdk Barb        HOLDS |
| `primitive_barbs` | **MET** | in the model, not in Rust (not a violation): BridgeAddress, BridgeCapNullifier, ChainDepositProof, DLEqProof, ExternalChain, IntentNullifier, RelayerCapability |
| `capability_type` | **MET** | no violations |
| `heavyweight_antipatterns` | **NOT MET** | [VIOLATION] empty-witnesses: /home/patrick/Darkfi/darkfi/src/contract/test-harness/src/harness/dex.rs:470 — empty_witnesses() in harness method — proves nothing about contract logic / [VIOLATION] empty-witnesses: /home/patrick/Darkfi/darkfi/src/contract/test-harness/src/harness/dex.rs:481 — empty_witnesses() in harness method — proves nothing about contract logic / [VIOLATION] empty-proof-stub: /h |
| `fee_guardrails` | **NOT MET** | === Fee System Guardrails === / [FI-GEN-2] No const/static of consensus domain types... PASS / [FI-GEN-2] No bare magic fee constants (1_001_000, 42_000_000)... PASS |
| `sync_conformance` | **MET** | === SYNC CONFORMANCE PASSED === |
| `heavyweight_coverage` | **NOT MET** | [GAP] promissory_note: 2/6 functions covered, accept_block=NO_ACCEPT_BLOCK. Missing: RedeemV1 IssueV1 RevokeV1 OtcSwapV1 / [GAP] deployooor: 1/2 functions covered, accept_block=NO_ACCEPT_BLOCK. Missing: LockV1 / [GAP] multisig: 1/4 functions covered, accept_block=NO_ACCEPT_BLOCK. Missing: CreateGroupV1 SignV1 FinalizeV1 |
| `length_cast_counter` | **NOT MET *(counter — exit 1 means sites remain, not a defect)*** | bearer_bond          10 / dex                  4 / pool_stake           1 |

### Which gates can fail

A check is not a check until something can make it fail (R8). Per gate script, whether it carries a control, and whether the umbrella runs that control:

| Gate script | control? | umbrella wires the gate? | umbrella wires the control? |
|---|---|---|---|
| `contrib/barb_alphabet_diff.sh` | **NO** | yes | — |
| `contrib/capability_type_diff.sh` | **NO** | yes | — |
| `contrib/ci/check_compiles.sh` | **NO** | n/a | — |
| `contrib/ci/check_fee_guardrails.sh` | yes | **no** | yes |
| `contrib/ci/check_heavyweight_coverage.sh` | **NO** | **no** | — |
| `contrib/ci/check_sync_conformance.sh` | yes | **no** | yes |
| `contrib/ci/scan_heavyweight_antipatterns.sh` | **NO** | yes | — |
| `contrib/genesis_model_conformance.sh` | **NO** | **no** | — |
| `contrib/primitive_barbs_diff.sh` | **NO** | yes | — |
| `contrib/test_verdicts.sh` | yes | n/a | yes |
| `contrib/wasm_artifact_check.sh` | **NO** | **no** | — |
| `scripts/check-artifact-freshness.sh` | **NO** | yes | — |
| `scripts/check-authority-resolves.sh` | yes | yes | yes |
| `scripts/check-circuit-domain-separation.sh` | **NO** | yes | — |
| `scripts/check-circuit-instance-derivation.sh` | **NO** | yes | — |
| `scripts/check-circuit-metadata-alignment.sh` | **NO** | yes | — |
| `scripts/check-client-params-alignment.sh` | **NO** | yes | — |
| `scripts/check-coinbase-classifier.sh` | **NO** | yes | — |
| `scripts/check-doc-index.sh` | **NO** | yes | — |
| `scripts/check-hidden-tests.sh` | **NO** | yes | — |
| `scripts/check-l1-wire-conformance.sh` | yes | yes | yes |
| `scripts/check-lean-suite.sh` | **NO** | yes | — |
| `scripts/check-metadata-arms.sh` | **NO** | yes | — |
| `scripts/check-phase-host-functions.sh` | **NO** | yes | — |
| `scripts/check-pubkey-binding.sh` | **NO** | yes | — |
| `scripts/check-register-artifacts.sh` | **NO** | yes | — |
| `scripts/check-wallet-kernel.sh` | **NO** | yes | — |
| `scripts/register-status.sh` | **NO** | yes | — |
| `scripts/validate_zk_bins.sh` | **NO** | yes | — |

## Documentation accuracy — the findings

This class is the one with no instrument, and it is where the load-bearing problems are. Each finding is recorded as an obligation rather than only here:

- **OBL-C147** (OPEN, severity M) — A document's statement that a build setting is in force is a statement about the tree: the mechanism the genesis specification and the artifact gate's own header both name is present, or the claim is withdrawn
- **OBL-C148** (OPEN, severity M) — A row's cited site is a site: the file, line numbers and symbols a row names still resolve, or the row is restated against what is there now
- **OBL-C149** (OPEN, severity M) — A register table's rows carry the columns its header declares, so a reader can locate a row's status and its severity in the columns the header names
- **OBL-C150** (CLOSED, severity M) — A parameter type's encoder and its decoder agree on the length they describe, so a call any client can build is a call the contract can decode

### Repairs made by this pass

Decision of record: findings are **recorded**, not fixed — a finding becomes an obligation and the repair is a separate decision. One exception, and it is not a judgement call: where the register has **already prescribed the correction** and the sentence it corrects is still standing, the repair is executing a recorded decision rather than making one (R11 — never leave a false statement in the tree).

- **`doc/src/arch/verification-hazop.md`, the "What this register implies" paragraph.** It read that `OBL-C80` "is now this register's **only open critical**". The register's own re-measurement, 30 lines below it, already says what it should read ("should read *only live critical*, `OPEN` being a status this row does not hold") — and the sentence was never changed. Applied, with its figures brought forward: that note measures 154 rows and `OBL-C80` at `PARTLY`; the register is now 197 rows and the row is `ACCEPTED-WITH-REASON`. The same paragraph's other overstated figure (`OBL-C82` as a new `H`) is annotated in place, since the note already measures it `M`. No row was minted for this: the register carried the correction, so a new row would restate one that exists.

### The genesis reproducibility claim, measured

`doc/src/arch/genesis.md`'s section "How the reproducibility clause is met" names two build settings and says that together they take all nine genesis contracts clean. The probes:

| Probe | Result |
|---|---|
| `strip` in any Cargo.toml | `NO HIT` |
| `git log -S 'strip = "symbols"' -- Cargo.toml` | `NO HIT on any commit` |
| rustflags in `.cargo/config.toml` |  |
| `git log -S 'location-detail' --all -- Makefiles` | `NO HIT on any branch` |
| `RUSTC_BOOTSTRAP` in the tree | `(removed 5 self-match(es): this script and its own previous output cite the pattern, and neither is part of the tree)` |
| contract Makefile RUSTFLAGS | `31:RUSTFLAGS += --remap-path-prefix=$(HOME)/.cargo=/cargo
32:RUSTFLAGS += --remap-path-prefix=$(HOME)/.rustup=/rustup
33:RUSTFLAGS += -C link-arg=--max-memory=268435456` |
| absolute host paths in the nine genesis wasms | `deployooor=0 native_token=0 promissory_note=0 identity=0 oracle=0 attestation=0 purse=0 box=0 multisig=0` |
| relative first-party paths in the nine | `deployooor=1 native_token=3 promissory_note=2 identity=0 oracle=0 attestation=0 purse=1 box=1 multisig=0` |

**Read the last two rows together, because they say different things and only both are true.** The gate's invariant (no marker strings, no first-party path) is breached; the purity property the clause is *about* (no host-dependence) is not, because every surviving path is relative. `OBL-C140` reads that split correctly. What no row records is that the mechanism the clause relies on is absent from the tree — and one of its two levers is unavailable on the pinned stable toolchain.

### The register census

`rows=198 unique=198 open=54 not_open=144
  by Status: ACCEPTED-WITH-REASON=11  CLOSED=59  DEFINITIONAL=1  FAILS=3  FIXED=13  MECHANIZED=2  OPEN=33  PARTLY=18  RESTATED=6  SATISFIED=52
  row tables: 4 distinct header shape(s) — 3x [ID | Status | Proposition | Enforced at | Checked today by | Sev] · 2x [ID | Status | Proposition | Source | Sev] · 1x [ID | Status | Proposition | Carried from | Sev] · 1x [ID | Status | Proposition | HAZID | Sev]
  cells: 60 of 198 rows carry a cell arity their own table header does not explain (a bare `|` inside a code span splits a GFM cell — the class `scripts/register-status.sh` documents and names six repaired rows of; it is not repaired)
  severity: read from its own column in 138 rows, recovered and marked in 60, unlocatable in 0`

Counts are from the **Status column**, never from `register-status.sh`'s token histogram, which the script itself documents as unreliable — the two currently disagree (histogram 55 OPEN against a census of 30) because the register's prose contains every status word in a non-status sense.

Open rows by severity:

- **C** (0)
- **H** (6): `OBL-C92*` OPEN, `OBL-C97*` PARTLY, `OBL-C133*` OPEN, `OBL-C143*` OPEN, `OBL-C145*` PARTLY, `OBL-C146*` OPEN
- **M** (44): `OBL-C12` PARTLY, `OBL-Z3` FAILS, `OBL-Z5*` PARTLY, `OBL-Z6` PARTLY, `OBL-Z12` PARTLY, `OBL-Z13` PARTLY, `OBL-Z18` FAILS, `OBL-T20` PARTLY, `OBL-T22` PARTLY, `OBL-T23` OPEN, `OBL-T26` OPEN, `OBL-C16` PARTLY, `OBL-C45` PARTLY, `OBL-C54` PARTLY, `OBL-C56` PARTLY, `OBL-C76` PARTLY, `OBL-C74` PARTLY, `OBL-C113*` OPEN, `OBL-C109*` FAILS, `OBL-C120*` OPEN, `OBL-C122*` PARTLY, `OBL-C82` PARTLY, `OBL-C91*` OPEN, `OBL-C95*` OPEN, `OBL-C96*` OPEN, `OBL-C102*` OPEN, `OBL-C123*` OPEN, `OBL-C125*` OPEN, `OBL-C126*` OPEN, `OBL-C127*` OPEN, `OBL-C130*` PARTLY, `OBL-C131*` OPEN, `OBL-C132*` OPEN, `OBL-C103*` OPEN, `OBL-C136*` OPEN, `OBL-C137*` OPEN, `OBL-C138*` OPEN, `OBL-C139*` OPEN, `OBL-C140*` OPEN, `OBL-C141*` OPEN, `OBL-C142*` OPEN, `OBL-C147` OPEN, `OBL-C148` OPEN, `OBL-C149` OPEN
- **L** (4): `OBL-T24` OPEN, `OBL-C89` OPEN, `OBL-C124*` OPEN, `OBL-C135*` OPEN

This report renders **no verdict on any open row's proposition** — it reports them as the register states them, with the instrument that read them. It also does not fix anything: a finding is recorded as an obligation, and the repair is a separate decision.

## What this report did not establish

Required section, not an appendix (R7 — "cannot verify" is a result, and a confident wrong answer is worse than an admitted unknown). Every NOT ESTABLISHED criterion, and the one command that settles it:

| ID | Not established | Settled by |
|---|---|---|
| G1 | The ceremony reads no ambient authority — no clock, RNG, network, or undeclared file | `read the three clauses at doc/src/arch/genesis.md:153-165 against the path` |
| G3 | The ceremony's stages are pure transitions whose composition is a pure transition | `read genesis.md:153-165 clause 2 against the stage boundaries` |
| G5 | Determinism — the same inputs yield the same hash | `cargo test -p dwowd --all-features test_genesis_determinism` |
| G8 | The pin is CURRENT — this build computes the hash the pin records | `cargo test -p dwowd --all-features genesis_pin_is_current` |
| G11 | The nine genesis contracts pass their heavyweight tests | `cargo test --release -p dwowd -- test_heavyweight_deployooor … (x9)` |
| C3 | The umbrella's Tier-1 gates are all green | `bash scripts/run-all-tests.sh` |
| C11 | The heavyweight contract suite is green | `make test  (or the targeted cargo test --release -p dwowd -- test_heavyweight_<c>)` |
| C12 | Finality is verified, not merely enforced | `read doc/src/arch/caribina.md's integration table against the callers` |

Beyond the criteria: this run made **no measurement of runtime behaviour at all**. It ran no test, compiled nothing, and proved nothing. Every verdict above is a statement about the tree as text, with the single exception of the instrument exits, which are statements about other static checkers.

## Appendix — reproducing this report

```sh
bash scripts/report-status.sh          # emits this report and the raw capture beside it
bash scripts/report-status.sh --self-test   # the negative control for the verdict logic
```

Raw, verbatim output per instrument: `reports/status-2026-09-26.raw/<name>.{out,err,exit}`.

Determinism: two consecutive runs must agree character for character except the date. If they do not, this generator is reading something non-deterministic and that thing must be named rather than averaged away.
