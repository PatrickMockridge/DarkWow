# Phase 1 Compliance Report — Uniform Runner Infrastructure
# Date: 2026-08-05
# Spec: doc/src/dev/testing/heavyweight-spec.md

## Compilation (RG-18)
Compilation: PASS (cargo check -p dwowd --lib --tests)

## Spec Sections Implemented
- §7.2 PR-1: strict_zk field removed from HeavyweightPipeline (commit 919e066)
- §7.2 PR-2 / §3.5: FeeCollectV1 unconditional (commit 919e066, revised in d6b08e4)
- §7.2 PR-3 / RG-8: State inspection API (commit 919e066, revised in d6b08e4)
- §7.2 PR-6: with_call() single signature, ZK gate in submit_block (commit d6b08e4)
- §5.1-5.8 / RG-7: Genesis deploy rejection (commit 919e066)
- §9: Uniform runner module — ContractTestSpec, EndpointSpec, run_heavyweight_test (commit 919e066)
- RG-9: Zkbin freshness checker (commit 919e066)
- RG-1: Design document — uniform-runner-design.md (commit 744a1fa)

## Guardrail Verification
- RG-5: grep -rn 'strict_zk' bin/dwowd/src/tests/ → ZERO hits (field removed)
- RG-6: with_fee_collect() always pushes to contract_txs — VERIFIED
- RG-7: deploy() rejects genesis names — VERIFIED
- RG-16: No compat_/_compat/bridge/shim methods — VERIFIED
- RG-17: No breaking API change without call site migration — PASS (with_call signature unchanged)
- RG-21: No heuristic-based ZK gating in with_call — VERIFIED

## Anti-Pattern Scan
Run: ./contrib/ci/scan_heavyweight_antipatterns.sh → 24 violations (PRE-EXISTING, not introduced in Phase 1)
These will be resolved in Phase 3 (test migration).

## Known Issues
- cumulative_supply() uses heuristics to read from supply_chain tree — needs proper API
- query_contracts_tree() is a simple key-value lookup on contracts tree — per-contract state trees not yet accessible
- Box test not yet migrated to uniform runner (Phase 3)

## Verification
./contrib/ci/check_compiles.sh → PASS
cargo check -p dwowd --lib --tests → PASS
git status --porcelain → CLEAN (RG-19)
