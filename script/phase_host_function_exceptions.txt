# Phase-host-function exceptions — the reviewed list for
# `scripts/check-phase-host-functions.sh`
#
# Format:  <file> : <exec-phase fn>() -> <host fn>() : <reason, citing a register ID>
#
# The key is the *content* of the call, not a line number, so an edit above a site moves nothing.
# `scripts/check-phase-host-functions.sh` reports a matching finding as EXCEPTED and does not fail
# the run on it; every other finding still fails. An entry that no longer matches anything is
# reported as stale, because a stale entry still admits the next finding of the same shape in the
# same function — that check was added on 2026-09-23 and is what proved this list was empty.
#
# WHAT BELONGS HERE. A site whose defect is understood, named and scheduled — the entry *is* the
# schedule, and it must cite a register ID (`doc/src/arch/verification-hazop.md`) and say what the
# repair is.
#
# WHAT DOES NOT BELONG HERE. A site nobody has looked at. Growing this file is the act that grants
# an exception, so an entry whose justification was not verified by reading the code is worse than
# a red gate — it is the defect wearing an audit badge.
#
# CURRENTLY EMPTY, and it was emptied by repairing the defect rather than by widening the list.
# The seven entries that used to be here were all `drain_protection`, all `() -> db_set()`, all one
# cause: the contract wrote its state from the exec phase, where every one of those calls returns
# `CALLER_ACCESS_DENIED` — `vm_runtime.rs:954` runs apply as `ContractSection::Update`, and no write
# function admits `Exec` at all — so as shipped the contract committed nothing anywhere.
#
# `OBL-C73` repaired it on 2026-09-23: each write moved into a new apply function beside a
# `process_update` that had been a no-op, and each update struct now **carries the value to write**,
# because apply may neither read nor validate (`§B.2.2`) and so cannot recompute it. That is why the
# repair was a contract rework and not a move: five of the seven updates held only identifiers, and
# there was nothing to write from. The gate found all seven entries stale in one run the moment the
# last write moved, which is the check that makes this file's emptiness a measurement rather than a
# claim.
