[package]
name = "darkwow"
version = "0.1.0"
edition = "2021"
description = "DarkWow top-level CLI — launches a node/observer, a wallet, or account operations"

[[bin]]
name = "darkwow"
path = "src/main.rs"

[dependencies]
dwow-accounts = { path = "../../crates/dwow-accounts" }
dwow-sdk = { path = "../../src/sdk" }
hex = "0.4.3"
